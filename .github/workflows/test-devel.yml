# This file was auto-generated by make_workflows.py.
# Do not edit it manually, modifications will be lost.

jobs:
  build-anope:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Anope
      uses: actions/checkout@v2
      with:
        path: anope
        ref: 2.0.9
        repository: anope/anope
    - name: Build Anope
      run: |-
        cd $GITHUB_WORKSPACE/anope/
        cp $GITHUB_WORKSPACE/data/anope/* .
        CFLAGS=-O0 ./Config -quick
        make -C build -j 4
        make -C build install
  build-ergo:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-go@v2
      with:
        go-version: ~1.16
    - run: go version
    - name: Cache dependencies
      uses: actions/cache@v2
      with:
        key: ${{ runner.os }}-ergo-devel
        path: |-
          ~/.cache
          $GITHUB_WORKSPACE/ergo
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Checkout Ergo
      uses: actions/checkout@v2
      with:
        path: ergo
        ref: master
        repository: ergochat/ergo
    - name: Build Ergo
      run: |
        cd $GITHUB_WORKSPACE/ergo/
        make build
        make install
    - name: Upload build artefacts
      uses: actions/upload-artifact@v2
      with:
        name: installed-ergo
        path: |-
          ~/.local
          ~/go
        retention-days: 1
  build-inspircd:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Checkout InspIRCd
      uses: actions/checkout@v2
      with:
        path: inspircd
        ref: master
        repository: inspircd/inspircd
    - name: Build InspIRCd
      run: |
        cd $GITHUB_WORKSPACE/inspircd/
        patch src/inspircd.cpp < $GITHUB_WORKSPACE/inspircd_mainloop.patch
        ./configure --prefix=$HOME/.local/inspircd --development
        make -j 4
        make install
    - name: Upload build artefacts
      uses: actions/upload-artifact@v2
      with:
        name: installed-inspircd
        path: |-
          ~/.local
          ~/go
        retention-days: 1
  build-limnoria:
    runs-on: ubuntu-latest
    steps:
    - name: Cache dependencies
      uses: actions/cache@v2
      with:
        key: ${{ runner.os }}-limnoria-devel
        path: |-
          ~/.cache
          $GITHUB_WORKSPACE/placeholder
    - name: Install dependencies
      run: pip install git+https://github.com/ProgVal/Limnoria.git@testing cryptography
    - name: Upload build artefacts
      uses: actions/upload-artifact@v2
      with:
        name: installed-limnoria
        path: |-
          ~/.local
          ~/go
        retention-days: 1
  build-solanum:
    runs-on: ubuntu-latest
    steps:
    - name: Cache dependencies
      uses: actions/cache@v2
      with:
        key: ${{ runner.os }}-solanum-devel
        path: |-
          ~/.cache
          $GITHUB_WORKSPACE/solanum
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Checkout Solanum
      uses: actions/checkout@v2
      with:
        path: solanum
        ref: main
        repository: solanum-ircd/solanum
    - name: Build Solanum
      run: |
        cd $GITHUB_WORKSPACE/solanum/
        ./autogen.sh
        ./configure --prefix=$HOME/.local/
        make -j 4
        make install
    - name: Upload build artefacts
      uses: actions/upload-artifact@v2
      with:
        name: installed-solanum
        path: |-
          ~/.local
          ~/go
        retention-days: 1
  build-sopel:
    runs-on: ubuntu-latest
    steps:
    - name: Cache dependencies
      uses: actions/cache@v2
      with:
        key: ${{ runner.os }}-sopel-devel
        path: |-
          ~/.cache
          $GITHUB_WORKSPACE/placeholder
    - name: Install dependencies
      run: pip install git+https://github.com/sopel-irc/sopel.git
    - name: Upload build artefacts
      uses: actions/upload-artifact@v2
      with:
        name: installed-sopel
        path: |-
          ~/.local
          ~/go
        retention-days: 1
  build-unrealircd:
    runs-on: ubuntu-latest
    steps:
    - name: Cache dependencies
      uses: actions/cache@v2
      with:
        key: ${{ runner.os }}-unrealircd-devel
        path: |-
          ~/.cache
          $GITHUB_WORKSPACE/unrealircd
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Checkout UnrealIRCd
      uses: actions/checkout@v2
      with:
        path: unrealircd
        ref: unreal52
        repository: unrealircd/unrealircd
    - name: Build UnrealIRCd
      run: |
        cd $GITHUB_WORKSPACE/unrealircd/
        cp $GITHUB_WORKSPACE/data/unreal/* .
        CFLAGS=-O0 ./Config -quick
        make -j 4
        make install
    - name: Upload build artefacts
      uses: actions/upload-artifact@v2
      with:
        name: installed-unrealircd
        path: |-
          ~/.local
          ~/go
        retention-days: 1
  publish-test-results:
    if: success() || failure()
    name: Publish Unit Tests Results
    needs:
    - test-charybdis
    - test-solanum
    - test-ergo
    - test-inspircd
    - test-inspircd-anope
    - test-unrealircd
    - test-unrealircd-anope
    - test-limnoria
    - test-sopel
    runs-on: ubuntu-latest
    steps:
    - name: Download Artifacts
      uses: actions/download-artifact@v2
      with:
        path: artifacts
    - name: Publish Unit Test Results
      uses: EnricoMi/publish-unit-test-result-action@v1
      with:
        files: artifacts/**/*.xml
  test-charybdis:
    needs:
    - build-charybdis
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-charybdis
    - name: Install Atheme
      run: sudo apt-get install atheme-services
    - name: Install irctest dependencies
      run: |-
        python -m pip install --upgrade pip
        pip install pytest -r requirements.txt
    - name: Test with pytest
      run: PYTEST_ARGS='--junit-xml pytest.xml' PATH=$HOME/.local/bin:$PATH  make
        charybdis
    - if: always()
      name: Publish results
      uses: actions/upload-artifact@v2
      with:
        name: pytest results charybdis (devel)
        path: pytest.xml
  test-ergo:
    needs:
    - build-ergo
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-ergo
    - name: Install Atheme
      run: sudo apt-get install atheme-services
    - name: Install irctest dependencies
      run: |-
        python -m pip install --upgrade pip
        pip install pytest -r requirements.txt
    - name: Test with pytest
      run: PYTEST_ARGS='--junit-xml pytest.xml' PATH=$HOME/.local/bin:$PATH  PATH={software_config['prefix']}/bin:$PATH
        make ergo
    - if: always()
      name: Publish results
      uses: actions/upload-artifact@v2
      with:
        name: pytest results ergo (devel)
        path: pytest.xml
  test-inspircd:
    needs:
    - build-inspircd
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-inspircd
    - name: Install Atheme
      run: sudo apt-get install atheme-services
    - name: Install irctest dependencies
      run: |-
        python -m pip install --upgrade pip
        pip install pytest -r requirements.txt
    - name: Test with pytest
      run: PYTEST_ARGS='--junit-xml pytest.xml' PATH=$HOME/.local/bin:$PATH PYTEST_ARGS="$PYTEST_ARGS
        -m 'not services'" PATH={software_config['prefix']}/bin:$PATH make inspircd
    - if: always()
      name: Publish results
      uses: actions/upload-artifact@v2
      with:
        name: pytest results inspircd (devel)
        path: pytest.xml
  test-inspircd-anope:
    needs:
    - build-inspircd
    - build-anope
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-inspircd
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-anope
    - name: Install Atheme
      run: sudo apt-get install atheme-services
    - name: Install irctest dependencies
      run: |-
        python -m pip install --upgrade pip
        pip install pytest -r requirements.txt
    - name: Test with pytest
      run: PYTEST_ARGS='--junit-xml pytest.xml' PATH=$HOME/.local/bin:$PATH PYTEST_ARGS="$PYTEST_ARGS
        -m 'not services'" PATH={software_config['prefix']}/bin:$PATH make anope
    - if: always()
      name: Publish results
      uses: actions/upload-artifact@v2
      with:
        name: pytest results anope (devel)
        path: pytest.xml
  test-limnoria:
    needs:
    - build-limnoria
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-limnoria
    - name: Install Atheme
      run: sudo apt-get install atheme-services
    - name: Install irctest dependencies
      run: |-
        python -m pip install --upgrade pip
        pip install pytest -r requirements.txt
    - name: Test with pytest
      run: PYTEST_ARGS='--junit-xml pytest.xml' PATH=$HOME/.local/bin:$PATH  make
        limnoria
    - if: always()
      name: Publish results
      uses: actions/upload-artifact@v2
      with:
        name: pytest results limnoria (devel)
        path: pytest.xml
  test-solanum:
    needs:
    - build-solanum
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-solanum
    - name: Install Atheme
      run: sudo apt-get install atheme-services
    - name: Install irctest dependencies
      run: |-
        python -m pip install --upgrade pip
        pip install pytest -r requirements.txt
    - name: Test with pytest
      run: PYTEST_ARGS='--junit-xml pytest.xml' PATH=$HOME/.local/bin:$PATH  make
        solanum
    - if: always()
      name: Publish results
      uses: actions/upload-artifact@v2
      with:
        name: pytest results solanum (devel)
        path: pytest.xml
  test-sopel:
    needs:
    - build-sopel
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-sopel
    - name: Install Atheme
      run: sudo apt-get install atheme-services
    - name: Install irctest dependencies
      run: |-
        python -m pip install --upgrade pip
        pip install pytest -r requirements.txt
    - name: Test with pytest
      run: PYTEST_ARGS='--junit-xml pytest.xml' PATH=$HOME/.local/bin:$PATH  make
        sopel
    - if: always()
      name: Publish results
      uses: actions/upload-artifact@v2
      with:
        name: pytest results sopel (devel)
        path: pytest.xml
  test-unrealircd:
    needs:
    - build-unrealircd
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-unrealircd
    - name: Install Atheme
      run: sudo apt-get install atheme-services
    - name: Install irctest dependencies
      run: |-
        python -m pip install --upgrade pip
        pip install pytest -r requirements.txt
    - name: Test with pytest
      run: PYTEST_ARGS='--junit-xml pytest.xml' PATH=$HOME/.local/bin:$PATH  PATH={software_config['prefix']}/bin:$PATH
        make unrealircd
    - if: always()
      name: Publish results
      uses: actions/upload-artifact@v2
      with:
        name: pytest results unrealircd (devel)
        path: pytest.xml
  test-unrealircd-anope:
    needs:
    - build-unrealircd
    - build-anope
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-unrealircd
    - name: Download build artefacts
      uses: actions/download-artifact@v2
      with:
        name: installed-anope
    - name: Install Atheme
      run: sudo apt-get install atheme-services
    - name: Install irctest dependencies
      run: |-
        python -m pip install --upgrade pip
        pip install pytest -r requirements.txt
    - name: Test with pytest
      run: PYTEST_ARGS='--junit-xml pytest.xml' PATH=$HOME/.local/bin:$PATH  PATH={software_config['prefix']}/bin:$PATH
        make anope
    - if: always()
      name: Publish results
      uses: actions/upload-artifact@v2
      with:
        name: pytest results anope (devel)
        path: pytest.xml
name: irctest with devel versions
'on':
  schedule:
  - cron: 51 8 * * 6
  - cron: 51 8 * * 0
  - cron: 51 17 * * *
  workflow_dispatch: null
